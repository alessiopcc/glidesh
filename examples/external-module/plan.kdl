plan "external-module" {
    mode "sync"

    vars {
        motd-message "Managed by glidesh. Unauthorized access prohibited."
    }

    step "Set MOTD" {
        external "example/motd" "${motd-message}"
    }

    step "Verify MOTD" {
        shell "cat /etc/motd"
    }
}
